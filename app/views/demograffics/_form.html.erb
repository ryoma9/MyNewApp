<%= form_with(model: demograffic, local: true) do |form| %>
  <% if demograffic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(demograffic.errors.count, "error") %> prohibited this demograffic from being saved:</h2>

      <ul>
      <% demograffic.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :demograffic_name %>
  </div>

  <div class="field">
    <%= form.label :age %>
    <%= form.number_field :age, id: :demograffic_age %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
